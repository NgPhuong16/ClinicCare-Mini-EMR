# Base URL of the FastAPI backend, including the /api/v1 prefix.
# Maps to runtimeConfig.public.apiBase in nuxt.config.ts; the default there is this value,
# so you only need a .env if the backend is somewhere else.
NUXT_PUBLIC_API_BASE=http://localhost:8000/api/v1
